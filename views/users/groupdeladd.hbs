
<form method="POST" action="/users/groupdeladd" class="col-6 m-auto">
  {{!-- <div class="mb-3">
    <label for="user-name" class="form-label">Имя</label>
    <input name="name" type="text " class="form-control" id="user-name" value="qwer">
    <div id="userNameHelp" class="form-text">Введите имя</div>
  </div>
  <div class="mb-3">
    <label for="inputEmail" class="form-label">Электронная почта</label>
    <input name="email" type="email" class="form-control" id="inputEmail" aria-describedby="emailHelp" value="qwer@slkd.qq">
    <div id="emailHelp" class="form-text">Введите электронную почту</div>
  </div>
  <div class="mb-3">
    <label for="inputPassword" class="form-label">Введите пароль</label>
    <input name="password" type="password" class="form-control" id="inputPassword" value="1234">
    <div id="passwordHelp" class="form-text">Введите пароль</div>
  </div> --}}
  <div class="mb-3 form-floating">
    <select name="roleBr" class="form-select" id="floatingSelect" aria-label="Floating label select example">
      <option selected>Бригадир доставки</option>
  {{#each brigadir}}
      <option value="{{this.name}}">{{this.name}}</option>
  {{/each}}
    </select>
    <label for="floatingSelect">Выберите бригадира</label>
  </div>
  <div class="mb-3 form-floating">
    <select name="roleDost" class="form-select" id="floatingSelect" aria-label="Floating label select example">
      <option selected>Доставщик</option>
  {{#each dostav}}
      <option value="{{this.name}}">{{this.name}}</option>
  {{/each}}
    </select>
    <label for="floatingSelect">Выберите доставщика</label>
  </div>
  <div class="mb-3 form-floating">
    <select name="roleDost2" class="form-select" id="floatingSelect" aria-label="Floating label select example">
      <option selected>Доставщик</option>
  {{#each dostav}}
      <option value="{{this.name}}">{{this.name}}</option>
  {{/each}}
    </select>
    <label for="floatingSelect">Выберите доставщика</label>
  </div>
  <div class="mb-3 form-check">
    {{!-- <input type="checkbox" class="form-check-input" id="exampleCheck1"> --}}
    {{!-- <label class="form-check-label" for="exampleCheck1">Запомнить пароль</label> --}}
  </div>
  <button type="submit" class="btn btn-primary">Submit</button>
</form>
