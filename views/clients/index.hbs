
<div class="wrapper m-5 mt-0 shadow-lg p-5 pt-5 bg-body rounded">
  <div class="d-flex justify-content-start mb-3">
    <a href="/clients/new" type="button" class="btn btn-primary">Добавить клиента</a>
  </div>
  <table class="table table-clients">
    <thead>
      <tr>
        <th scope="col">ID</th>
        <th scope="col">ФИО</th>
        <th scope="col">address</th>
        <th scope="col">Телефон</th>
        <th scope="col">Заказы</th>
      </tr>
    </thead>
    <tbody class="table-hover">
      {{#each clients}}
      <tr data-clientid={{this.id}} class="shadow-lg p-3 mb-5 bg-body rounded">
        <td scope="row">{{this.id}}</td>
        <td>{{this.surName}} {{this.name}} {{this.lastName}}</td>
        <td>{{this.adress}}</td>
        <td>{{this.telephone}}</td>
        <td>Заказы</td>
      </tr>
      {{/each}}
    </tbody>
  </table>
</div>

